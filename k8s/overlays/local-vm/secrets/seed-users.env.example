# Copy to seed-users.env (git-ignored) and fill in a real value, or run:
#   scripts/bootstrap-secrets.sh local-vm
# Password assigned to the seeded demo users on realm import. Each has
# "temporary": true, so Keycloak forces a password change on first login —
# this value only needs to work once.
SEED_USER_PASSWORD=change-me
