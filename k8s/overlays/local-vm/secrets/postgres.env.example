# Copy to postgres.env (git-ignored) and fill in real values, or just run:
#   scripts/bootstrap-secrets.sh local-vm
POSTGRES_PASSWORD=change-me
KEYCLOAK_PASSWORD=change-me
MERCHANT_PASSWORD=change-me
